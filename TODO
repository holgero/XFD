* convert usb service routine to interrupt
  curiosity: wanna see if that works too
* add a boot loader, to enable updates via USB
* update HID description:
** remove LEDs that are no longer present in the pic18f13k50 variant
** specify answers from the device
* move from HID to vendor specific class
* create footprint for mini usb socket and make a layout with it
* device with brighter LEDs that can draw 500mA from the bus
* device with brighter LEDs that is self-powered
